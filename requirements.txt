# Enhanced Flask Calculator System - Production Dependencies
# Install with: pip install -r requirements.txt

# Core Flask Framework
flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0

# Environment and Configuration
python-dotenv==1.0.0

# AI and OpenAI Integration
openai==1.3.8

# Database and Storage
tinydb==4.8.0

# Optional: Redis for enhanced caching (if using Redis instead of TinyDB for rate limiting)
redis==5.0.1

# Data Processing and Validation
requests==2.31.0

# System Monitoring (Optional but recommended for production)
psutil==5.9.6

# Development and Testing (Optional)
pytest==7.4.3
pytest-flask==1.3.0

# Production Server (Choose one)
gunicorn==21.2.0        # For Linux/Mac production
waitress==2.1.2         # For Windows production

# Security (Optional but recommended)
cryptography==41.0.8

# Logging (Optional but recommended for production)
python-json-logger==2.0.7

# Performance Monitoring (Optional)
werkzeug==3.0.1